# vertical-slice-plan

입력 VS ID를 `contracts/features/vertical-slices.yaml`에서 찾는다. Page→Action/API→domain transition→table/RLS→AI graph→test ID 순서의 trace matrix를 만든다. RLS, immutable version, rights/consent, audit/provenance, loading/error/a11y를 acceptance criteria에 포함한다. 미확정 decision은 구현 가정으로 바꾸지 않는다.

