# ai-contract-change

Graph/node/prompt/model/schema 변경을 독립적으로 수행하지 않는다. registry와 version을 갱신하고 golden set을 동일 조건으로 재실행한다. hidden reasoning·raw provider payload·PII를 artifact에 저장하지 않는다. 사용자 확인 없는 canonical write와 AI-only rejection을 테스트한다.

출력: implementation plan, registry diff, schema validation, golden-set diff, AI evaluation report.

